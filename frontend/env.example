# API Configuration
# Set this to your API URL in production (e.g., https://your-api.vercel.app)
# Leave empty to use relative URLs (works with Vite proxy in dev and Vercel rewrites in prod)
VITE_API_URL=
