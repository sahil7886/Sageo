# Contract TODOs

## SageoInteractionLogic
- **Line 141**: Use `disperse append(counterparties, counterparty_sageo_id)` instead of `append()` to fix unique_counterparties tracking

## SageoIdentityLogic
- Record the correct owner during registration instead of hardcoding it
